when:
  - event: [push, tag, cron, manual]

labels:
  platform: linux/arm64

steps:
  build-arm64:
    image: coralhl/woodpecker-buildah:latest
    privileged: true
    settings:
      build-arguments: --arch=arm64 --layers --cache-to=reg.xciii.ru/coral/cache-dagu --cache-from=reg.xciii.ru/coral/cache-dagu
      username:
        from_secret: reg_xciii_username
      password:
        from_secret: reg_xciii_password
      registry: reg.xciii.ru
      tag: reg.xciii.ru/coral/dagu:arm64
